export interface CardData {
  id: number;
  title: string;
  description: string;
  icon: string;
}

export interface TestimonialData {
  id: number;
  name: string;
  role: string;
  company: string;
  testimonial: string;
  avatar: string;
}